What story do I want to tell with the work done in this repo?

Missing pieces

Start with the glide-path arm
- Should show that the bisection + gradient-ascent algorithm converges to similar paths with random starting points
- If it does, show that we're capable of highly overfitting the data if we want to
- Then show that a regularized variant produces a much more reasonable solution with a tiny cost
- Apply to the retirement arm to validate that the approach yields comparable quality to professional advice (Vanguard + Fidelity)
